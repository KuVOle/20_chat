<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>message</title>
</head>
<body style="background-color:lightslategray;">
    <div style="width: 100vw; height: 60px; background: gray; display: flex; justify-content: space-around;">
        <div style="display: flex; justify-content:center">
            <button class="btn" name="settings">"<%= who %>"</button>
        </div>
        <button class="btn btn-error" name="logout">Logout</button>

    </div>     
    <div class="container">
        <div class="row align-items-start">
            <div class="col">
                <form method="post" class="d-flex flex-row">
                    <div>
                        <input class="form-control" type="text" name="newContact" placeholder="новый собеседник">
                    </div>
                    <div>
                        <button name="addNewContact" class="btn btn-primary">добавить</button>
                    </div>
                </form>
                <%if(contacts.length != 0){%>
                <% contacts.forEach( el => {%>
                    <div>
                        <a class="btn" href="/message/<%= el.id%>"><%= el.login%></a>
                    </div>
                <%})}%>

            </div>

            <div class="col" style="overflow-y: scroll; width: 70vw; height:80vh; background-color: #dde; border: 1px solid black;">
               <%if(messages?.length != 0){ %>
                <% messages.forEach(el => { %>
                        <div <%if(el.who != who){%>style="display: flex; justify-content:flex-end"<%}%>>
                            <p style="border: 1px solid green; border-radius: 5px; display: inline-block;"><%= el.message%></p>
                        </div>
                <%})}%>
              </div>
        </div>
    
        <div class="fixed-bottom">
            <form method="post">
                <div class="py-2 d-flex bd-highlight">
                    <div class="p-2 flex-grow-1 bd-highlight" >
                        <input class="form-control" type="text" name="message" placeholder="send message">
                        </div>
                    <div class="p-2 bd-highlight">
                        <button class="btn btn-success" name="send">Отправить</button>
                   </div>
                </div>
            </form>
        </div>
       
    </div>
</body>
</html>